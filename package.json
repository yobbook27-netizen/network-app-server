{
  "name": "network-app-server",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal backend proxy for the network-app AI features (Roadmap generation, Resume critique/JD tailoring). Keeps the Anthropic API key server-side.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
